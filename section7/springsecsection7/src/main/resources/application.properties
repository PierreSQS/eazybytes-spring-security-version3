# ENABLED COLOR ON CONSOLE OUTPUT
# NOT NECESSARY IN OUR CONTEXT.
# See logging.pattern.console
#spring.output.ansi.enabled=ALWAYS

spring.application.name=${SPRING_APP_NAME:eazybankbackend}
logging.level.org.springframework.security=${SPRING_SECURITY_LOG_LEVEL:TRACE}

spring.datasource.url=jdbc:mysql://${DATABASE_HOST:localhost}:${DATABASE_PORT:3308}/${DATABASE_NAME:eazybank}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:Galerien3?}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${HIBERNATE_FORMAT_SQL:true}

# ENABLE SQL-SYNTAX HIGHLIGHTING
spring.jpa.properties.hibernate.highlight_sql=${HIBERNATE_highlight_sql:true}

logging.pattern.console = ${LOGPATTERN_CONSOLE:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n}

# SESSION TIMEOUT
server.servlet.session.timeout=${SESSION_TIMEOUT:20m}
